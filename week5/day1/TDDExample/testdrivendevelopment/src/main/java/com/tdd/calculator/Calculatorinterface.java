package com.tdd.calculator;

public interface Calculatorinterface {
    // All interface methods are a public abstract class by default
    int addition(int numOne, int numTwo);

    int subtraction(int numOne, int numTwo);

    int mutliplication(int numOne, int numTwo);

    int division(int numOne, int numTwo);
}
